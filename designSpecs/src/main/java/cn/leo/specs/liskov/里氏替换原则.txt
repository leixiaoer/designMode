由麻省理工学院姓里的一位女士提出的一种原则：
    尽量少修改父类的方法，但是还是有可能在无意识的修改了父类的方法，导致调用方法有误会；同时重写父类方法会导致复用性变差；特别是多态运行比较频繁；
    那么用里氏的思想，我们可以将继承拆分，采用依赖、聚合、组合关系替代；
    我们将两个对象都继承一个类，然后在原本的子类中，注入原本的父类对象，要使用原本的父类对象方法的时候，调用注入的对象的方法即可；

    将原本的继承关系，变成组合关系，需要使用的父类的方法，可以使用依赖注入调用父类的方法；

    当然我感觉也可以使用super和this关键字；